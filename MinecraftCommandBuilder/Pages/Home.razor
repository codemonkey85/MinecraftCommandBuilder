@page "/"

<PageTitle>
    Minecraft Command Builder
</PageTitle>

<MudStack Class="mt-3">
    <CopyToClipboardComponent CommandText="@CommandText" />

    <MudButton OnClick="@ChangeCommandText"
               ButtonType="@ButtonType.Button"
               Variant="@Variant.Filled"
               title="Change command text">
        Change command text
    </MudButton>

    <MudSelect T="Block"
               Placeholder="Select a block"
               Clearable>
        @foreach (var block in Blocks.OrderBy(b => b.displayName).Take(10))
        {
            <MudSelectItem T="Block"
                           Value="@block">
                @block.displayName
            </MudSelectItem>
        }
    </MudSelect>

    <MudSelect T="Effect"
               Placeholder="Select an effect"
               Clearable>
        @foreach (var effect in Effects.OrderBy(e => e.displayName).Take(10))
        {
            <MudSelectItem T="Effect"
                           Value="@effect">
                @effect.displayName
            </MudSelectItem>
        }
    </MudSelect>

    <MudSelect T="Item"
               Placeholder="Select an item"
               Clearable>
        @foreach (var item in Items.OrderBy(i => i.displayName).Take(10))
        {
            <MudSelectItem T="Item"
                           Value="@item">
                @item.displayName
            </MudSelectItem>
        }
    </MudSelect>

    <MudSelect T="Biome"
               Placeholder="Select a biome"
               Clearable>
        @foreach (var biome in BiomesList.OrderBy(i => i.displayName).Take(10))
        {
            <MudSelectItem T="Biome"
                           Value="@biome">
                @biome.displayName
            </MudSelectItem>
        }
    </MudSelect>

    <MudSelect T="Entity"
               Placeholder="Select an entity"
               Clearable>
        @foreach (var entity in EntitiesList.OrderBy(i => i.displayName).Take(10))
        {
            <MudSelectItem T="Entity"
                           Value="@entity">
                @entity.displayName
            </MudSelectItem>
        }
    </MudSelect>

</MudStack>
